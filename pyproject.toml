[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["old_odev.py", "static"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]  # long SQL / command strings are clearer unwrapped
